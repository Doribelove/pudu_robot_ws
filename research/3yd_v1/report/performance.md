# 3YD-V1 静态规划实验与 3YD-V0-r0 性能对照

正式版本 **3YD-V1** 对应轻量 L1＋0.15 m L2（含必要的 0.05 m L2 回退），实验标识为 `topology_015`；基线为 **3YD-V0-r0**。本文数据来自本轮同条件复测，命名确认未改变原始数据或冻结实现。

## 已实现的链路

topology → 0.15 m 参考规划 → 世界坐标参考线 → 0.05 m 软代价输入 → 原 Smac → 双重轨迹检查。

L1 用语义子区域和有向接口做按需搜索，取消成对 Dubins 连接及详细轨迹预计算。L2 保守聚合原精细障碍/安全可通行性，保留方向规则，生成偏好参考线。粗图端点不可表示、搜索无路或超时时可切到所选路线区域的 0.05 m L2；完整回退成本计入查询。

L3 原始占据图、Smac、车辆和规划参数保持不变；新的输入仍通过原有软参考适配、实际代价内容确认、原 PathAudit 及独立连续车体检查。

接口定义见 [INTERFACES.md](/home/robot/workspaces/3yd_multires_static_20260914/INTERFACES.md)。

## 查询性能

相同真实地图、20 条冻结查询、每条 3 次，三组独立进程在 CPU 15 顺序运行。下表耗时单位为秒，单元格为 **P50 / P95**。所有查询均保留在成功分母和总耗时统计中。

| 指标 | 当前 3YD-V0-r0 | 轻量 L1＋0.05 m L2 | 3YD-V1 |
| --- | --- | --- | --- |
| 成功 / 总次数 | 60 / 60 | 60 / 60 | 60 / 60 |
| 查询总耗时 | 4.711 / 16.814 | 2.112 / 17.074 | 1.792 / 15.626 |
| 首次查询总耗时（20 次） | 15.417 / 20.521 | 15.768 / 18.285 | 4.606 / 16.140 |
| 重复查询总耗时（40 次） | 4.425 / 5.019 | 1.932 / 2.599 | 1.597 / 2.629 |
| L1 路线请求 | 2.29096 / 2.57543 | 0.00005 / 0.00122 | 0.00005 / 0.00124 |
| 首次 L2 初始化 | 9.653 / 11.300 | 12.151 / 15.368 | 1.496 / 13.029 |
| 重复 L2 初始化 | 0.725 / 0.989 | 0.435 / 0.600 | 0.097 / 0.758 |
| L3 参考代价适配 | 0.742 / 0.891 | 0.727 / 1.292 | 0.680 / 0.875 |
| 地图更新与内容确认 | 0.018 / 1.699 | 0.018 / 1.738 | 0.018 / 2.105 |
| Smac 搜索与平滑 | 0.101 / 0.265 | 0.105 / 0.301 | 0.086 / 0.290 |
| 地图内容校验后重发修复次数 | 2 | 1 | 3 |
| 进程树 CPU 时间 | 4.660 / 16.297 | 1.965 / 16.541 | 1.755 / 15.100 |

首次查询指本查询的 L2 状态和参考缓存尚未命中，L1 图已加载；它不代表操作系统冷启动。查询计时包含 L1、L2、L3 和轨迹检查，路径质量计算、轨迹与统计文件落盘发生在计时之后。原有内容校验的重发修复及其耗时均保留，因此部分长耗时也来自地图更新。各阶段分位数不能直接相加。
新接口将精细区域通道的生成计入 L2 初始化，而基线在 L1 中生成走廊。因此 L1 分项不能单独用作整条链路提速倍数，主结论依据完整查询计时。

全部查询的总耗时中位数从 4.711 s 变为 1.792 s，变化 -62.0%；中位数之比为 2.63 倍。
首次查询的总耗时中位数从 15.417 s 变为 4.606 s，变化 -70.1%；中位数之比为 3.35 倍。
重复查询的总耗时中位数从 4.425 s 变为 1.597 s，变化 -63.9%；中位数之比为 2.77 倍。

`topology_005` 同时包含移除 Dubins 引导后新增的栅格引导路与偏好缓存，因此它与基线的差值代表轻量静态链路整体改造的收益。两个新接口分组之间才用于观察 L2 分辨率的影响。

## 粗图回退与实际收益边界

3YD-V1 中，42 / 60 次 L2 保持粗图并完成最终规划，18 / 60 次回退到精细 L2；涉及 6 / 20 条不同查询。最终 60 / 60 成功包含回退的贡献，纯粗图 L2 完成率为 70%，L3 始终使用 0.05 m。基线同样为 60 / 60，因此该实验表明固定样本中的成功率得到保持，不能声称成功率提高。

回退查询：cand2-06b-lane-to-parking、cand2-07a-parking-to-lane、cand2-08b-parking-internal、cmp2-06-lane-to-parking、cmp2-07-parking-to-lane、cmp2-08-parking-internal。

当前回退在整个选定路线区域使用 0.05 m L2，尚未压缩成局部精细桥接。它能保留窄连接的可达性，也会保留部分原有搜索成本。所有回退与先行失败搜索都已计时。

同面积总格数从 **8,594,760** 降至 **955,420**；每类同型密集 L2 数组约减少 **88.9%**。L3 全图、软代价适配、地图内容确认及服务内存仍存在，整机收益不按九分之一推算。

在 14 条无精细回退的首次查询中，同一新接口的 L2 中位数为：0.05 m 12.227 s，0.15 m 1.332 s。

## 构图与内存

构图使用空的应用缓存；恢复使用另一个新进程。文件系统页缓存没有人为清空。两种图存储的对象不同：基线是已验证车辆连接，新图是待后续验证的有向语义接口。

| 分组 | L1 构建与落盘 s | L1 缓存恢复 s | 图缓存 MiB | 准备阶段峰值 RSS GiB | 节点 / 连接或接口 |
| --- | --- | --- | --- | --- | --- |
| 当前 3YD-V0-r0 | 258.335 | 4.715 | 2,544.936 | 6.967 | 7880 / 216039 |
| 轻量 L1＋0.05 m L2 | 0.826 | 0.103 | 0.875 | 0.495 | 2508 / 2508 |
| 3YD-V1 | 0.818 | 0.103 | 0.875 | 0.431 | 2508 / 2508 |

计数口径：新组上表的 2508 / 2508 表示接口状态节点数 / 有向区域接口数。按同一区域内入接口到出接口展开，实际有 **22,024 条有向候选搜索边**；这些边尚未逐条离线验证车辆轨迹，不能与旧版 216,039 条已验证车辆连接作等价比较。

| 资源指标 | 当前 3YD-V0-r0 | 轻量 L1＋0.05 m L2 | 3YD-V1 |
| --- | --- | --- | --- |
| 查询进程树峰值 RSS（GiB） | 3.608 | 2.808 | 2.714 |
| 查询结束进程树 PSS 最大值（GiB） | 3.021 | 2.400 | 2.345 |
| Smac 进程峰值 RSS（GiB） | 0.852 | 0.850 | 0.848 |
| L2 状态＋几何最大值（MiB） | 5.356 | 18.034 | 18.034 |

进程树 RSS 按约 20 ms 间隔采样，会重复计入共享页；PSS 按比例分摊共享页。此处 PSS 是每次查询结束的采样最大值，不是连续峰值。L2 状态项不包含全图、偏好场、L3 与 ROS 服务。
精细回退时，新拓扑区域通道比基线的轨迹走廊更宽，L2 状态最大值反而增加；总进程内存的下降主要来自移除旧图的详细连接数据，不能解释为所有内存项都减少。

## 轨迹与机制检查

| 指标 | 当前 3YD-V0-r0 | 轻量 L1＋0.05 m L2 | 3YD-V1 |
| --- | --- | --- | --- |
| 被接收但独立检查不通过 | 0 | 0 | 0 |
| 路径长度 P50 / P95（m） | 50.413 / 56.486 | 51.876 / 56.829 | 51.688 / 55.774 |
| 公共细图靠右目标带占比 P50 / P95 | 91.1 / 99.8% | 91.7 / 99.9% | 91.9 / 100.0% |
| 参与偏好统计的轨迹次数 | 60 | 60 | 60 |
| 去除两端后有效语义长度 P50（m） | 29.163 | 28.263 | 29.238 |

真实地图偏好统一用基线的 0.05 m 语义参考评估场衡量，按 0.025 m 弧长采样，排除两端各 5 m 与歧义/不适用区域。各组使用同一评价定义，并报告有效长度；不同路线的数值不能替代受控偏好因果验证。目标带为安全归一化横向区间 [0.125, 0.375]。

新粗图组与基线成对比较，路径长度比中位数为 0.9993，P95 为 1.0937，最大为 1.0948；部分查询的路径增加约 9.5%，因此成功率与速度改善并不代表每条路线都更短。

逐查询质量补充：`cmp2-08-parking-internal` 与 `cand2-08b-parking-internal` 的路径长度分别增加约 9.48% 和 9.36%，0.05 m 中间对照组也出现这一现象。两条查询在去除不适用区域后的有效车道段中，两版靠右目标带占比均为 0%，不是指整个停车区都需要靠右。`cmp2-05-junction-turn` 的目标带占比约从 91.9% 降到 87.7%。因此整体中位数接近基线，不代表所有真实查询的靠右质量都理想。

两个新接口分组的精细硬通道哈希在 60 / 60 个对应查询上相同。

| 受控场景 | 分组 | 成功次数 | 左右目标带最低占比 | 切换期间硬通道一致 |
| --- | --- | --- | --- | --- |
| calibration | 当前 3YD-V0-r0 | 9/9 | 100.0% | True |
| calibration | 轻量 L1＋0.05 m L2 | 9/9 | 100.0% | True |
| calibration | 3YD-V1 | 9/9 | 100.0% | True |
| rotated | 当前 3YD-V0-r0 | 9/9 | 100.0% | True |
| rotated | 轻量 L1＋0.05 m L2 | 9/9 | 100.0% | True |
| rotated | 3YD-V1 | 9/9 | 100.0% | True |

窄通道最终 L3 轨迹通过 3 / 3 次，全部保留精细回退原因及连续车体检查记录。最终回归测试通过 51 项，覆盖坐标、边界补齐、保守障碍、方向/禁转、缓存完整性、加权搜索一致性和窄通道回退。

开发试跑曾发现语义边界截断车体转弯余量，独立检查拒绝了这些结果；随后统一改为由车辆与最小转弯半径计算 0.85 m 区域余量。开发失败记录保留在 `real_pilot_01`、`turn_fix_probe_01`，正式数据来自冻结后运行，未删除失败重算分母。

最终跨进程恢复检查还发现区域字典整数键经 JSON 读取后排序不同，导致缓存完整性误报。修正仅位于拓扑构造函数的缓存读取分支，正式查询组在空图缓存上构图，没有执行该分支；查询、通道生成与 L2/L3 算法未改变。修复后重新测量两组新图的构建/恢复，并对缓存恢复后的两组各 20 条查询逐一核对：拓扑签名、精细硬通道、L2 参考点列与实际分辨率均和原计时样本相同。详情见 [缓存恢复一致性验证](/home/robot/workspaces/3yd_multires_static_20260914/report/cache_restore_validation.json)。

## 后续优先事项

下一批建议先定位停车区回退、额外绕行及路口偏好不足，实施局部精细连接，并补齐首选候选路线不可行时的 L1 备选路线恢复。验收同时覆盖成功率、独立轨迹检查、配对路径长度、最差偏好查询、完整回退比例和 P95。之后优化软参考输入处理，再推进 L2/L3 同源动态更新和 L1 通道状态与安全切换。详见 [3YD-V1 下一阶段建议](/home/robot/workspaces/3yd_multires_static_20260914/NEXT_STAGE_PLAN.md)。

本轮结果证明这些固定静态查询和受控场景中的链路与偏好响应，不代表动态避障、任意起终点完备性或实机控制验收。

## 可追溯文件

- [原始机器可读统计](/home/robot/workspaces/3yd_multires_static_20260914/report/performance.json)
- [逐轨迹偏好数据](/home/robot/workspaces/3yd_multires_static_20260914/report/path_quality.json)
- [正式运行脚本](/home/robot/workspaces/3yd_multires_static_20260914/tools/run_static_formal.bash)
- [正式查询冻结哈希](/home/robot/workspaces/3yd_multires_static_20260914/report/IMPLEMENTATION_FREEZE.json)
- [缓存修正后的交付哈希](/home/robot/workspaces/3yd_multires_static_20260914/report/FINAL_IMPLEMENTATION_FREEZE.json)
- [源文件与输入一致性核查](/home/robot/workspaces/3yd_multires_static_20260914/report/integrity.json)
- [最终回归测试结果](/home/robot/workspaces/3yd_multires_static_20260914/report/regression_cache_fix.xml)
- 三组正式逐次记录位于 `results/formal_01_baseline`、`results/formal_01_topology_005`、`results/formal_01_topology_015`。
